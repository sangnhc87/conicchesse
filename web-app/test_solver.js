const { execSync } = require('child_process');
// actually, I can just console.log in the frontend, but I need to see it.
// I will patch `CheckmateSolverModal.jsx` to `console.log` all failures and successful mate checks.
